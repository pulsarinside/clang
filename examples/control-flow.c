#include <stdio.h>

int main()
{
    // Control flows are if, else if, switch,
    int *ptr = NULL;
    if (!ptr)
    {
        printf("false");
        // This condition is false because ptr is a null pointer.
    }
}